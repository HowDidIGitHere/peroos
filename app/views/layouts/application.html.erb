<!DOCTYPE html>
<html>
  <head>
    <title>Peroos</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Linked-In photo -->
    <meta property="og:title" content="Peroos"/>
    <meta property="og:image" content="<%= asset_path('peroos_gif.gif') %>"/>
    <meta property="og:description" content="Peroos, a Reddit clone, is a social media application that allows its users to connect with each other based on shared interests."/>
    <meta property="og:url" content="https://peroos-jamie.herokuapp.com/#/"/>
    <meta name="author" content="Jamie An" />

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= favicon_link_tag asset_path('p.png') %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
